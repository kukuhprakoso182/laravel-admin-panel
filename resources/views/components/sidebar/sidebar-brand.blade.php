<header class="sidebar__header">
    <a class="sidebar__brand" href="{{ route('dashboard') }}">
        <x-ui.icon name="sparkling-2-fill" />
        <span>{{ config('app.name', 'Meridian') }}</span>
    </a>
</header>
